Section goal
- You are introducted to google colab
- You will learn how to use google colab
- We explain how to enable GPU in google colab. And that we need to do this every time we start a new session
- Then we run some very simply commands to see GPU is attached to our runtime
- We also run some commands to see that PyTorch can see the GPU
- We also run some commands to see that PyTorch can run some operations on the GPU
- We stop here. We don't do any heavy operations. We don't do any training. We don't do any deep learning. 